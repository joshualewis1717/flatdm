export default function NewListingsPage() {
  return (
    <div className="space-y-6">
      <section className="rounded-[2rem] border border-white/10 bg-white/[0.03] p-6 sm:p-8">
        <p className="text-xs font-medium uppercase tracking-[0.35em] text-primary/85">Listings</p>
        <h1 className="mt-3 text-3xl font-semibold tracking-tight text-white sm:text-4xl">
          My Listings page (delete all this)
        </h1>
        <p className="mt-4 max-w-3xl text-sm leading-7 text-white/68">
          For landlords only, this page just displays a list of all the listings the current landlord has created. 
        </p>
      </section>
    </div>
  );
}
